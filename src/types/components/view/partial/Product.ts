export interface ProductData {
	category: string;
	title: string;
	description: string;
    image: string;
    price: string;
}

export interface ProductSettings {
    category: string;
	title: string;
	description: string;
	compactClass: string;
	isCompact: boolean;
    image: string;
    price: string;
}
